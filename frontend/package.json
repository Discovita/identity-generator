{
  "name": "frontend-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start": "cd apps/identity-generator && npm start",
    "build": "cd apps/identity-generator && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}